{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "lldb",
            "request": "launch",
            "name": "Debug zigra",
            "program": "${workspaceFolder}/zig-out/bin/zigra",
            "args": [],
            "cwd": "${workspaceFolder}",
        }
    ]
}
